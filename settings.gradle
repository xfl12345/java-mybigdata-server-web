pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://repo.spring.io/release' }
        mavenLocal()
    }

    plugins {
        id 'org.springframework.experimental.aot' version '0.12.1'
    }
}

rootProject.name = 'server-web'
